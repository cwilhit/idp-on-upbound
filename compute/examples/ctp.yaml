apiVersion: spaces.upbound.io/v1beta1
kind: ControlPlane
metadata:
  name: compute
  namespace: default
  labels:
    dimension.scheduling.upbound.io/offering: "compute"
spec:
  crossplane:
    autoUpgrade:
      channel: Rapid